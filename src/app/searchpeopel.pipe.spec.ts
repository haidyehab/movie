import { SearchpeopelPipe } from './searchpeopel.pipe';

describe('SearchpeopelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpeopelPipe();
    expect(pipe).toBeTruthy();
  });
});
